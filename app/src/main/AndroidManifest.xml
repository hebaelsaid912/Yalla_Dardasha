<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.final_graduation_project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission." />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="audioid.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:resizeableActivity="false"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_Graduation_Project"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.home.fragments.profile.following.FollowingActivity"></activity>
        <activity android:name=".ui.home.fragments.profile.follwers.FollowersActivity" />
        <activity android:name=".ui.home.fragments.rooms.get_room_info.ActiveRoomActivity" />
        <activity android:name=".ui.intro.IntroSplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.phone_verifying.createUser.CreateUserActivity" />
        <activity android:name=".ui.phone_verifying.sendOtp.RegisterActivity" />
        <activity
            android:name=".ui.splash_screen.SplashActivity"
            android:configChanges="orientation|screenSize" />
    </application>

</manifest>